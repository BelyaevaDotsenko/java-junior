к константам добавить PREFIX (PRIMITIVE_PREFIX)

ArrayList or LinkedArrayList??

ans, cnt --> full name

extract methods

remove lambdas

printBuffer: remove typeSwitching (make overloading)

printXXX methods: extract common code (make common method + branching: print(const, methodId), where:
    const = INT_MAX_VALUE or BYTE_MAX_VALUE,
    methodID = 0 (for function A), 1 (for function B), ...
    ==> parametrization

.getClass -- remove (we try to avoid reflectionAPI)

assert -- remove